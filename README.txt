# Flashcard Generator using LLM (GPT)

This tool converts educational text into question-answer flashcards using GPT-3.5-turbo.

## How to Run

```bash
pip install -r requirements.txt
streamlit run app.py